const price=63722.2,amount=document.getElementById('from-amount'),output=document.getElementById('to-amount'),from=document.getElementById('from-currency'),to=document.getElementById('to-currency'),summary=document.getElementById('summary-value'),note=document.getElementById('wallet-note'),modal=document.getElementById('modal'),toast=document.getElementById('toast');const value=()=>Number(amount.value.replace(/[^0-9.]/g,''))||0;const format=(n,c)=>c==='USD'?`$${n.toLocaleString('en-US',{minimumFractionDigits:2,maximumFractionDigits:2})}`:`${n.toFixed(8)} BTC`;function update(){if(from.value===to.value)to.value=from.value==='USD'?'BTC':'USD';const converted=from.value==='USD'?value()/price:value()*price;output.value=to.value==='USD'?converted.toFixed(2):converted.toFixed(8);summary.textContent=format(converted,to.value);note.textContent=from.value==='USD'?'From your NIXXA USD wallet':'From your NIXXA Bitcoin wallet'}amount.addEventListener('input',update);from.addEventListener('change',update);to.addEventListener('change',update);document.getElementById('swap').addEventListener('click',()=>{const oldFrom=from.value;from.value=to.value;to.value=oldFrom;amount.value=output.value;update()});document.querySelectorAll('.quick button').forEach(b=>b.addEventListener('click',()=>{amount.value=b.dataset.value==='max'?(from.value==='USD'?'4826.40':'0.07574300'):b.dataset.value;update()}));document.getElementById('review').addEventListener('click',()=>{if(value()<=0)return;modal.classList.remove('hidden');document.getElementById('modal-content').innerHTML=`<h2>Review conversion</h2><p>You convert</p><strong>${format(value(),from.value)}</strong><p>You receive <b>${format(Number(output.value),to.value)}</b></p><button class="confirm" id="confirm">Confirm conversion</button>`;document.getElementById('confirm').addEventListener('click',()=>{modal.classList.add('hidden');toast.textContent='Conversion initiated';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)})});document.getElementById('close').addEventListener('click',()=>modal.classList.add('hidden'));modal.addEventListener('click',e=>{if(e.target===modal)modal.classList.add('hidden')});document.getElementById('help').addEventListener('click',()=>{toast.textContent='NIXXA support is ready to help';toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)});update();
