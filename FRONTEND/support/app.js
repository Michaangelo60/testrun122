const toast=document.getElementById('toast');
document.querySelectorAll('[data-message]').forEach(button=>button.addEventListener('click',()=>{toast.textContent=button.dataset.message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)}));
const chatForm=document.getElementById('chat-form');
if(chatForm){const input=document.getElementById('message-input');const conversation=document.getElementById('conversation');chatForm.addEventListener('submit',event=>{event.preventDefault();const text=input.value.trim();if(!text)return;const bubble=document.createElement('div');bubble.className='message user';bubble.textContent=text;conversation.append(bubble);input.value='';conversation.scrollIntoView({block:'end',behavior:'smooth'});setTimeout(()=>{const reply=document.createElement('div');reply.className='message bot';reply.textContent='Thanks for the details. A NIXXA specialist will review this and reply shortly.';conversation.append(reply);window.scrollTo(0,document.body.scrollHeight)},650)})}
