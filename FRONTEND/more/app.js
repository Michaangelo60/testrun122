const toast=document.getElementById('toast');const modal=document.getElementById('modal');const notify=message=>{toast.textContent=message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),1800)};document.querySelectorAll('[data-message]').forEach(button=>button.addEventListener('click',()=>notify(button.dataset.message)));document.getElementById('settings').addEventListener('click',()=>notify('Settings opened'));document.getElementById('logout').addEventListener('click',()=>modal.classList.remove('hidden'));document.getElementById('stay').addEventListener('click',()=>modal.classList.add('hidden'));document.getElementById('confirm').addEventListener('click',()=>{modal.classList.add('hidden');notify('You have been logged out')});modal.addEventListener('click',event=>{if(event.target===modal)modal.classList.add('hidden')});
